# Hyperagent Local Proxy — runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
# Optional — only needed for browser (CDP) auth mode. In config-session mode
# (SESSION_MODE=config / HYPERAGENT_SESSION) the proxy runs without it.
playwright>=1.44
